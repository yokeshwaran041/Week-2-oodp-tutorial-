#include <iostream>
#include <cmath> // For round()

int main() {
    double pi = 3.14159;
    int approx_pi = (int)round(pi); // Round to the nearest integer
    std::cout << approx_pi;
    return 0;
}
